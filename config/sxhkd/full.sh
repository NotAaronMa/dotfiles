if [ -z "$(bspc query -N -n focused.fullscreen)" ]; then                   
	bspc node focused -t fullscreen;                                   
else                                
	
	~/.config/sxhkd/state.sh                                
fi
